openapi: 3.0.3
info:
  title: Referral API
  description: REST API для створення та оновлення обʼєкта Referral__c
  version: 1.0.0

servers:
  - url: /services/apexrest/mobile

paths:
  /referral:
    post:
      summary: Create Referral
      tags: [Referral]
      description: Створення нового запису Referral__c
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReferralCreateRequest'
      responses:
        '200':
          description: Referral successfully created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReferralResponse'
        '400':
          description: Validation error
        '500':
          description: Internal server error

    put:
      summary: Update Referral
      tags: [Referral]
      description: Оновлення існуючого Referral__c по referralExternalId
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReferralUpdateRequest'
      responses:
        '200':
          description: Referral successfully updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReferralResponse'
        '404':
          description: Referral not found
        '500':
          description: Internal server error

components:
  schemas:
    ReferralCreateRequest:
      type: object
      required:
        - referralExternalId
        - senderContactExternalId
        - senderContactRole
        - status
        - invitationDate
        - invitedContactFullName
        - invitedContactPhone
        - invitedContactRole
      properties:
        referralExternalId:
          type: string
          description: Унікальний зовнішній ідентифікатор Referral

        senderContactExternalId:
          type: string
          description: External Id контакту, який відправив запрошення

        senderContactRole:
          type: string
          enum:
            - Консультант
            - Фермер
          description: Роль контакту, який відправив запрошення
  
        status:
          type: string
          enum:
            - Sent
            - Registered
            - Rejected
          description: Статус реферального запрошення

        invitationDate:
          type: string
          format: date-time
          description: Дата та час відправки запрошення

        registrationDate:
          type: string
          format: date-time
          nullable: true
          description: Дата та час реєстрації користувача

        invitedContactFullName:
          type: string
          description: Повне імʼя запрошеного користувача

        invitedContactPhone:
          type: string
          description: Телефон запрошеного користувача

        invitedContactRole:
          type: string
          enum:
            - Консультант
            - Фермер
          description: Роль запрошеного користувача

    ReferralUpdateRequest:
      type: object
      required:
        - referralExternalId
        - status
      properties:
        referralExternalId:
          type: string
          description: Унікальний зовнішній ідентифікатор Referral

        status:
          type: string
          enum:
            - Registered
            - Rejected
          description: Новий статус реферального запрошення

        registrationDate:
          type: string
          format: date-time
          description: Дата та час реєстрації користувача 

        registeredContactExternalId:
          type: string
          nullable: true
          description: External Id контакту, який зареєструвався

    ReferralResponse:
      type: object
      properties:
        externalId:
          type: string
          description: External Id Referral__c
        success:
          type: boolean
          example: true
        created:
          type: boolean
          description: true — якщо запис створено, false — якщо оновлено
        message:
          type: string
          example: Referral created successfully
